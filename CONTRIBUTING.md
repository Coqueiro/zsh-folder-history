# Contributing

- Keep changes focused.
- Run `zsh -n zsh-folder-history.plugin.zsh` before opening a PR.
- Run `zsh tests/run.zsh` before opening a PR.
- Update docs if behavior changes.
- Open a pull request with a short explanation of the change and why it is needed.
